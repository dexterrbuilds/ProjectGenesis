# Prospective class-consistent context arm

Added after generic L0/L1 and some L2 runs, BEFORE ANY class-consistent simulation.
No original run/result/plan is replaced. The generic context operator has maximum
absolute incoming gain .05 while context KCs have threshold .15. Therefore added
KCs cannot become active from neural input in that arm, regardless of restored
connectivity. This follows from the equations, not a desired behavioral outcome.
It makes generic-only negative closure evidence insufficient for the user's question.

Add a separately labeled class-consistent arm with exactly the existing frozen
class gains, fixed across every network level. No fitted gain or desired behavior.
Added PN (ALPN)→KC: original pn_gain3; added KC→original MBON populations or APL:
gain1; APL→KC: original apl_gain1. Source identities use original roles for original
roots and known anatomical cell classes/types for new roots. No arbitrary new DAN
teaching or peptide/receptor effect. Other context still has recurrent_gain.05;
ACh/GABA signs as original context, unresolved signs zero. Keep original core
weights, plastic edges and all inputs exactly fixed. New KC→MBON edges are fixed
anatomical transmission, not newly invented plastic compartments.

Run both cues at L0–L3; Stage3 both states. L0 identity is already verified and is
reused, not rerun. At L2 repeat relevant DA silence, frozen plasticity, original
comparator, context-cut, half/double context gains, sign variant and normalization
alternative. Record that scalar half/double multiplies the entire ADDED class
operator, with original core unchanged. These are sensitivity arms, not retuning.
All completed generic runs remain as a deliberately conservative control family.

To avoid wasted sparse storage, remove exactly zero entries in the NEW class
operator. This changes representation, not numerical weights. Check exact short
trajectory equivalence before/after zero elimination before its main battery.
All neural steps still execute. No silent temporal decimation.

This arm may strengthen, weaken, invert or abolish any effect. It cannot revise a
frozen stage's acceptance. Report effects separately from the generic family. Any
conclusion must account for the physiological distinction between operators.
