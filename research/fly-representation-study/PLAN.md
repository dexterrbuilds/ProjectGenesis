# Neural representation and minimal physiology — registered analysis

Scope: additive representation study only. All accepted studies, C. elegans and canonical Genesis remain frozen. No Stage 4, integration or behavioral optimization. Earlier results and published figures are already known; this is not prospective biological preregistration or analyst blinding.

## Comparisons fixed before fitting

1. APL: preserve the previous Figure 4 train/validation split and result. Compare its one-state, reciprocal observation-gain model (R0) with two anatomically named states (calyx/lobe) sharing one leakage coefficient q (R1), a zero-leakage nested control, and the earlier two-direction fit as a complexity comparator. Fit only original training rows. No recording-specific fit parameters. Evaluate the already-used validation rows as reused validation, never call them a new independent test. Bootstrap whole training recordings and leave-one-recording-out training prediction. Use training AICc including residual variance, explicitly approximate because two directions within each recording are dependent. Prefer zero/one coefficient unless the extra coefficient materially improves held-out prediction. A 20% MSE reduction is a study convention for material improvement, not a biological threshold.
2. Challenge spatial generalization with independently acquired Amin Figure 3 paired alpha/alpha-prime measurements (different indicator/stimulation). Predict only the normalized off-site/on-site transfer conditional on measured on-site response. Freeze Figure 4 fits first. This is an assay shift, not proof of transferable membrane parameters. Exclude no negative measurements. Bootstrap entire recordings within driver groups, not individual entries. Record where the two-region approximation fails.
3. MBON14 (alpha3, NOT alpha-prime3 or MBON07/11): train a population passive-time-constant prediction from the published five-cell table, validate against the separately acquired four-cell cytosolic-EGFP supplementary table. Fix that split before reading supplementary numbers. These summary measurements can test cross-preparation prediction, but cannot discriminate scalar vs cable response trajectories. Seek raw voltage traces; if inaccessible, report unavailable rather than generate pseudo-observations. Do not fit R3's many conductances to five summary constants.
4. Independently audit PN/KC sparsity and timing, transmitter/receptor signs, local dopamine, timing-dependent plasticity, recovery and conditioning. Keep cell types, observation modalities and preparations separate. Do not turn qualitative sign constraints into invented quantitative targets. R2/R3 are instantiated only where data identify the added physiological degrees of freedom; otherwise provide explicit unidentifiability/observational-equivalence results.
5. No neural-kernel transfer or Stage replay unless both representation AND parameter/observation mappings become independently supported. Assay-level structural support alone is insufficient. Never rerun historical stages as pseudo-calibrated validation.
6. Define one root-string identity, local-state ownership, canonical aggregate anatomical-row identity and one plastic state per selected row. Never represent aggregated proofread pair/neuropil counts as individual synapse IDs. Reconcile shared-row identity only; leave incompatible update rules unresolved.
7. After specifying representation, benchmark explicit engineering envelopes on pinned real minimal, ~38k and full-reference anatomy. R0 scalar; R1 occupied neuropil-local states; R2 local states plus illustrative receptor/modulator/eligibility/plastic buffers. Unknown physiological parameters are NOT calibrated. Report memory, every-update throughput, checkpoints and dt-dependent costs; no real-time claim for unidentified R3. Include richer morphology storage bounds separately.

## Selection and limitations

Choose a minimum representation only within the domain actually predicted. Global final classification requires evidence across multiple independent phenomena for a transferable representation; APL-only support is explicitly narrower. If no common representation reaches that standard: INSUFFICIENT EVIDENCE TO SELECT REPRESENTATION, while retaining the positive assay-specific result. Do not interpret inability to compare as equality or failure of biology.

Primary source workbooks are read-only, provenance-hashed. Prior APL validation has already been inspected. The new Figure 3 and MBON14 supplementary numbers are uninspected when this plan is written. All results, including deterioration, are retained.
