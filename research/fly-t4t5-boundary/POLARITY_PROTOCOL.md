# Direct 2021 polarity/RF descriptive extraction

Registered before extraction of voltage response values from the newly completed T4 member. This is not a refit of the already frozen conditional T5 candidates. Process both contrast values, every actual position/width/duration, using the author's `subData.baseSub(:,2)` and `zeroInd`, at 20 samples/ms. Preserve source means and trial counts. Do not treat summary rows as recordings. T4 cell ordinal 5 is skipped by the author plotting code; retain it as an explicit exclusion and audit whether it contains data. No inference that a cell ordinal is a fly.

Save actual mean traces in a -100 to 600 ms window at 1 ms spacing (take every 20th original sample, not a fitted observation conversion). Compute descriptive mean, minimum and maximum on the original 20 kHz mean within [0,duration+75 ms), plus pre-stimulus [-100,0) RMS. Group by cell, width, duration, contrast and source position. Author-reported RF peak and inhibition-side alignment may be saved as conditional descriptive coordinates; do not use either to solve anatomy→screen or tune a boundary. Where both polarities exist at the author's center, report paired values. No p-value or biological calibration inferred from extrema of noisy traces; no arbitrary pass threshold. Measurement equations cannot transform voltage into calcium or spikes.

If the complete T5 member is recovered, apply the same extraction without a T4-cell-5 exclusion. Reused 2019 T5 recordings are not an independent replication. Sparse-bar calcium, isolated anatomical-column flicker and T4 apparent-motion results remain distinct data families.
