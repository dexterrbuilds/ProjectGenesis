# α1 → UpWiN identity and mediation resolution

Start with [REPORT.md](REPORT.md). `READINESS.json` contains the ten requirement ratings and separate root/type/population decisions. `IDENTITY_CROSSWALK.json`, `SOURCE_IDENTITY_AUDIT.json`, `TRANSMISSION.json` and `MEDIATION.json` preserve evidence scope and unknowns.

`DEPENDENCIES.json` pins Brain Spec v0.2 and the frozen route-acquisition release. `RELEASE.json` seals this separate study. Nothing is admitted into Brain Spec.

## Verify without running a biological experiment

From the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 research/fly-upwin-identity-mediation/validate.py
```

This uses the Python standard library, reads protected artifacts and the saved read-only database audit, and does not connect to the runtime or database. Tests include adversarial identity promotion, sign/receptor invention, false replication, unit conversion, chronic/acute mediation overclaim, missing provenance, anatomy ownership, execution and capability promotion.

Acquisition/analysis scripts record how this package was produced. Do not rerun builders against a sealed release. Publisher original byte ranges are retained because the whole 117 MiB archive request timed out. `ARCHIVE_DIRECTORY.json` and `ARCHIVE_AUDIT.json` specify offsets, CRCs, SHA-256s and the 36 byte-identical source workbooks. No raw trials were fabricated. `KEY_RESOURCES_TEXT.txt` is derived from the original supplementary DOCX; it is a reagent table, not a raw-data manifest.

Stage 5 remains unauthorized. No neural simulation, parameter fitting, decoder, BrainAdapter change or canonical write occurred.
