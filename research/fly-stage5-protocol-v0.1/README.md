# Stage-5 prospective protocol — design only

Read [STAGE5_PROTOCOL.md](STAGE5_PROTOCOL.md). This package contains no Stage-5 results. Execution remains unauthorized.

Design documents are machine-readable JSON beside the protocol. `STAGE1_INPUT_LOCK.json` references 28 original primary appetitive runs and 56 snapshots. `TRANSFER_FAMILIES.json` and `ASSUMPTION_PARTITIONS.json` distinguish evidence-admissible, restricted conditional and engineering scopes.

`prospective_analysis.py` is an **unexecuted** analysis implementation. Do not import it, run its functions or preview its output during protocol review. No neural model will be run even by the proposed future mathematical analysis.

Safe design verification only:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 validate_design.py
```

Run this from this directory. The verifier checks hashes, schemas, coverage and static AST; it does not import prospective analysis, read source-summary numerical values, run neural tests, or generate Stage-5 outputs.

The sealed `RELEASE.json` pins all documents/code. Any amendment requires a separately reviewed new version. The execution checklist requires explicit authorization naming that hash; protocol readiness itself is not authorization.
