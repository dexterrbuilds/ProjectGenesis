# Minimal transfer identifiability

Read `REPORT.md` for the result and its conditional scope. No physiological transfer is selected or calibrated. Stage 5 remains unauthorized.

This package uses only frozen Brain Spec v0.2, Stage 1, route-acquisition and identity/mediation evidence. `DEPENDENCIES.json` pins them; `PRESERVATION_BEFORE.json` protects previous bytes. No new literature is added. Prior canonical identity/synapse ownership is referenced, never duplicated.

`analyze.py` performs finite-source arithmetic and checks mathematical equivalence witnesses. It does not simulate neural dynamics, fit physiological parameters or import Stage-1 model code. Its illustrative numbers are dimensionless mathematical coordinates, not biological parameter ranges or a proposed Stage-5 grid.

Read-only validation from the repository root:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 research/fly-upwin-transfer-identifiability/validate.py
```

Do not rerun builders against a sealed release. Final `RELEASE.json` contains per-file hashes and a content-addressed package digest. Database auditing used the existing read-only audit script and no life cycles were run.
