# Frozen transfer decision and replay plan

Recorded after the independent fits/identifiability audit and before replay or comparison to historical numerical outcome tables.

**No neural parameter transfer is justified.** APL fluorescence coefficients cannot set inhibitory weights; γ1pedc aggregate depression cannot determine learning rate or be transferred to α1. Therefore no calibrated kernel is exported and no calibrated-capability prediction is claimed.

Execute a separately labeled **null-transfer replay** of the frozen original protocols for first original seed, both cue directions in each stage, and both resource states in Stage 3 (8 runs total). Keep their exact equations, parameters, extractions and stimuli. Use original deterministic replay and snapshot-restore checks and save genuine traces into this study only. This is a preservation/control arm, not a calibrated-physiology result. It does not rerun entire completed validation batteries.

The worker reads only original model/protocol/anatomical files and the frozen transfer decision; historical summaries are read by a separate comparison step only after all new runs are complete. The analyst knows prior classifications and approximate effects; no analyst blinding is claimed. There is no optimization or parameter choice using replay output.

Calibrated Stage-1/2/3 effect changes remain `not identifiable / not evaluated` under the transfer gate, rather than being filled with this unchanged-model arm's values. No post-replay fitting is allowed.
