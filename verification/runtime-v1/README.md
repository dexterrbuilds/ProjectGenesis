# Runtime V1 verification

Sanitized implementation verification only; no biological research or capability admission. Private database exports, credentials and the original user authorization remain excluded. See the five root Runtime V1 reports.

`MANIFEST.json` hashes the raw bytes of each included file; `manifestContentSha256` hashes compact sorted JSON of that file map. This is an implementation-verification identity, not a Brain Spec or research release identity.
