import pg from 'pg';import {OneShotController} from '../../server/one-shot.ts';import {ProductFallback} from '../../core/v2/planner.ts';
if(process.env.DATABASE_URL||new URL(process.env.TEST_DATABASE_URL!).pathname!=='/genesis_runtime_v1_test'||!/^awakening_test_[a-f0-9]+$/.test(process.env.FIXTURE_SCHEMA??''))throw Error('Fixture only');
const pool=new pg.Pool({connectionString:process.env.TEST_DATABASE_URL,options:`-c search_path=${process.env.FIXTURE_SCHEMA}`});const h=new OneShotController(pool);const phase=process.argv[2];if(phase==='before_claim')process.exit(77);const f=await h.claim('fixture-grant');if(phase==='after_claim')process.exit(77);const p=await h.prepare(f,{identity:'product-deterministic-v1',planner:new ProductFallback()});if(phase==='after_intent')await h.journal(f,p.decision);process.exit(77);
