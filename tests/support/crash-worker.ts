import pg from 'pg';import {IsolatedCycle} from '../../server/isolated-cycle.ts';import {ProductFallback} from '../../core/v2/planner.ts';import {CLOSED_PERMISSIONS} from '../../core/v2/identity.ts';
if(process.env.DATABASE_URL||new URL(process.env.TEST_DATABASE_URL!).pathname!=='/genesis_runtime_v1_test'||!/^awakening_test_[a-f0-9]+$/.test(process.env.FIXTURE_SCHEMA??''))throw new Error('Fixture only');
const pool=new pg.Pool({connectionString:process.env.TEST_DATABASE_URL,options:`-c search_path=${process.env.FIXTURE_SCHEMA}`});const h=await IsolatedCycle.connect(pool);const f=await h.claim('crash');const result=await h.prepare(f,new ProductFallback(),{...CLOSED_PERMISSIONS,execution:true});if(process.argv[2]==='after_intent')await h.journal(f,result.decision);process.exit(77);
